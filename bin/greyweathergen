#!/usr/bin/ruby -I ./lib -s

# arbitrary defaults: Weather will be generated for 1st of Planting

$month ||= 4

require 'YAML'
require 'GreyhawkWeatherGenerator'

weather = GreyhawkWeatherGenerator.new.generate $month.to_i

weather.dump
